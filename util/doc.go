/*
Package util contains several helper functions for finding files.

They are in this subpackage because they are not necessarily useful
for workflow authors, but may be, so they're exported and importable,
but not by default.

*/
package util
